# 📚 Паттерны проектирования на TypeScript

## О проекте

Этот репозиторий представляет собой примеры реализаций паттернов проектирования с реализацией на TypeScript. Проект создан для понимания классических паттернов программирования и их применения в реальных задачах разработки.

Каждый паттерн реализован с понятными и простыми комментариями. Старался как мог.

Проект охватывает три основные категории паттернов: порождающие (Generative), структурные (Structural) и поведенческие (Behavioral).

---

## 📂 Структура проекта

```
patterns/
│
├── 📁 Generative/                    # Порождающие паттерны
│   ├── Factory_Method.ts             # Фабричный метод
│   ├── Abstract_Factory.ts           # Абстрактная фабрика
│   ├── Builder.ts                    # Строитель
│   ├── Singletone.ts                 # Одиночка
│   └── Prototype.ts                  # Прототип
│
├── 📁 Structural/                    # Структурные паттерны
│   ├── Adapter.ts                    # Адаптер
│   ├── Bridge.ts                     # Мост
│   ├── Composite.ts                  # Компоновщик
│   ├── Facade.ts                     # Фасад
│   └── Proxy.ts                      # Заместитель
│
├── 📁 Behavioral/                    # Поведенческие паттерны
│   ├── ChainOfCommand.ts             # Цепочка обязанностей
│   ├── Command.ts                    # Команда
│   ├── Mediator.ts                   # Посредник
│   └── Iterator.ts                   # Итератор
└──
```

---

## 🎯 Категории паттернов

### 🏭 Порождающие паттерны (Generative)

Паттерны, которые решают задачи создания объектов и управления их инстанцированием.

#### [`Factory_Method.ts`](Generative/Factory_Method.ts)
**Фабричный метод** — определяет общий интерфейс для создания объектов, позволяя подклассам изменять тип создаваемых объектов.

**Пример использования:**
- Создание различных типов страховок (ABInsurance, XYInsurance)
- Универсальная фабрика с типизированным выбором продукта
- Общая логика работы с базой данных для всех типов

#### [`Abstract_Factory.ts`](Generative/Abstract_Factory.ts)
**Абстрактная фабрика** — создает семейства связанных объектов без указания их конкретных классов.

**Пример использования:**
- Кросс-платформенные UI компоненты (Windows/Mac)
- Создание кнопок и чекбоксов для разных операционных систем
- Общая логика через базовые классы (ButtonBase)

#### [`Builder.ts`](Generative/Builder.ts)
**Строитель** — пошаговое создание сложных объектов с возможностью получения различных представлений.

#### [`Singletone.ts`](Generative/Singletone.ts)
**Одиночка** — гарантирует существование только одного экземпляра класса и предоставляет глобальную точку доступа.

#### [`Prototype.ts`](Generative/Prototype.ts)
**Прототип** — создание новых объектов путем копирования существующих экземпляров.

---

### 🏗️ Структурные паттерны (Structural)

Паттерны, которые решают задачи компоновки классов и объектов в более крупные структуры.

#### [`Adapter.ts`](Structural/Adapter.ts)
**Адаптер** — позволяет объектам с несовместимыми интерфейсами работать вместе.

**Пример использования:**
- Интеграция старого класса OldSquare в новую систему
- Адаптер наследует интерфейс NewSquare и делегирует вызовы старому классу
- Бесшовная подмена без изменения существующего кода

#### [`Bridge.ts`](Structural/Bridge.ts)
**Мост** — разделяет абстракцию и реализацию, позволяя им изменяться независимо.

#### [`Composite.ts`](Structural/Composite.ts)
**Компоновщик** — группирует объекты в древовидные структуры для представления иерархий часть-целое.

#### [`Facade.ts`](Structural/Facade.ts)
**Фасад** — предоставляет простой интерфейс к сложной системе классов.

#### [`Proxy.ts`](Structural/Proxy.ts)
**Заместитель** — контролирует доступ к другому объекту, перехватывая обращения к нему.

---

### 🎭 Поведенческие паттерны (Behavioral)

Паттерны, которые решают задачи эффективного взаимодействия между объектами.

#### [`ChainOfCommand.ts`](Behavioral/ChainOfCommand.ts)
**Цепочка обязанностей** — передает запрос по цепочке обработчиков, где каждый решает, обработать запрос или передать дальше.

**Пример использования:**
- Middleware система для обработки HTTP запросов
- Последовательная проверка: аутентификация → валидация → контроллер
- Гибкое добавление и удаление обработчиков в цепочке

#### [`Command.ts`](Behavioral/Command.ts)
**Команда** — инкапсулирует запрос как объект, позволяя параметризовать клиентов с различными запросами, ставить запросы в очередь и поддерживать отмену операций.

**Пример использования:**
- Система управления пользователями с возможностью отката (undo)
- Разделение отправителя и получателя команды
- История выполненных команд для отмены действий
- Переиспользование бизнес-логики через команды

#### [`Mediator.ts`](Behavioral/Mediator.ts)
**Посредник** — определяет объект, который инкапсулирует взаимодействие множества объектов, избавляя их от необходимости ссылаться друг на друга напрямую.

**Пример использования:**
- Система управления воздушным движением (диспетчер и пилоты)
- Компоненты общаются только через посредника
- Уменьшение связанности между компонентами
- Централизованное управление взаимодействием

---

## 📖 Дополнительные ресурсы

- [Годный ресурс для изучения паттернов](https://refactoring.guru/ru/design-patterns)
- [Дока по TS](https://www.typescriptlang.org/docs/handbook/intro.html)

---